var {User} = require('./../models/users');

var generateUsers = () => {
    return new Promise((resolve, reject)=>{
        User.find({}).then((users) => {
            resolve(users);
        }, (err) => {
            reject(err);
        });
    });
};


module.exports = {
    generateUsers
}
